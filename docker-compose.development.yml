services:
  email-db:
    env_file: .env
    image: postgres:15
    ports:
      - '5432:5432'
    restart: 'unless-stopped'

  rabbitmq:
    image: rabbitmq
    ports:
      - "5672:5672"
      - "15672:15672"
    restart: 'unless-stopped'
